# 文档分区规则（AI 必读）

## 目标
- 指导性内容与临时内容分离，避免干扰主线文档。

## 放置规则
- `docs/`：指导性文档（长期有效、指导产品/设计/规则/技术/商业）。
- `notes/`：临时/协作/执行类文档（会议记录、调试记录、临时讨论、交接说明）。

## 新建文档前的判断
- 是否长期有效、需要反复参考？→ 放 `docs/`
- 是否只为当前协作/执行阶段服务？→ 放 `notes/`

## 强制要求
- AI 生成新文档时必须遵守上述规则。
